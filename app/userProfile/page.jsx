// Funcționalități principale:

// Afișarea datelor curente ale utilizatorului (nume, email, username, etc.).
// Formular pentru actualizarea datelor (schimbare nume, email, parolă).
// Posibilitatea de a schimba parola curentă (prin introducerea parolei vechi, apoi a celei noi și confirmarea acesteia).
// Validări la nivel de client și server pentru a asigura integritatea datelor (ex.: verificare parolă curentă, validare email, etc.).
